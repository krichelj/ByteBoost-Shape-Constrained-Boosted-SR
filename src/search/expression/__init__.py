"""
Expression-tree operators and protocols for corrections ``g`` (``sec:boosting``).

Primary operator set ``{+, −, ×, ÷} ∪ {pow_p}``; optional unary ablations.
Trees are the objects DualInterval certificates and soft scores act on.

See ``operators.py``, ``tree.py``.
"""
